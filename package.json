{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server -w db.json --port 3004",
    "sass": "node-sass -w scss/ -o css/ -r "
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "json-server": "^1.0.0-alpha.23",
    "node-sass": "^9.0.0",
    "swiper": "^11.1.1"
  }
}
